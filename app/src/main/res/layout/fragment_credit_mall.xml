<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="happyyoung.trashnetwork.recycle.ui.fragment.CreditMallFragment">

    <com.malinskiy.superrecyclerview.SuperRecyclerView
        android:id="@+id/commodity_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:mainLayoutId="@layout/layout_recyclerview_verticalscroll"
        app:scrollbarStyle="insideInset"
        app:layout_moreProgress="@layout/recyclerview_loading"/>

    <com.malinskiy.superrecyclerview.SuperRecyclerView
        android:id="@+id/commodity_search_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:mainLayoutId="@layout/layout_recyclerview_verticalscroll"
        app:scrollbarStyle="insideInset"
        app:layout_moreProgress="@layout/recyclerview_loading"
        android:visibility="gone"/>

    <TextView
        android:id="@+id/txt_no_commodity"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:drawableStart="@drawable/ic_info_outline_48dp"
        android:layout_centerInParent="true"
        android:gravity="center"
        android:textSize="@dimen/large_text_size"
        android:text="@string/no_commodity"
        android:visibility="gone"/>

</RelativeLayout>
